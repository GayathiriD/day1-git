name: Day7 PR CI Test v2

on:
  push:
    branches: ["master", "main"]
  
  pull:
    branches: ["master","main"]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Success message
        run: echo "CI passed Successfully"
      
    

